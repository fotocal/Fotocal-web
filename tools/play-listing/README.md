# Play listing text (2026-09-14)

`listing-en.txt` and `listing-es.txt` are paste-ready: the short and the
full description for the Play Console store listing, English and
Spanish (Spain). The Premium block inside each is the nine lines of
`tools/premium_features.json`, word for word, so the listing, the
website and the app's paywall say the same thing. When that file changes,
regenerate these two (the generator is inline in the commit that added
them; rebuild by hand from the JSON if needed) and paste again.

The trial sentence names no plan on purpose: it is true whether the trial
is on the yearly plan only or on both. If Play Console confirms yearly
only, "on the yearly plan" can be added after "5-day free trial".

What the paste must REPLACE, not sit beside: see the checklist in the
session report of 2026-09-14 (also in tools/APP-BUGS.md, item 0g).
